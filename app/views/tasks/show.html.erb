<h1><%= @task.title %></h1>
<%= @task.details %></p>

<p><%= @task.complited ? "☑️ This task is completed" : "☐ This task is not completed yet" %>
<br>
<br>
<%= link_to "Edit task", edit_path(@task.id) %>

<br>
<br>

<%= link_to "Back to list", tasks_path %>
